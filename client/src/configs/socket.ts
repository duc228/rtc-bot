// import { ENV } from "../constants/env";

// // const URL = ENV.SERVER_URL_WS || "localhost:5000";
// const URL = ENV.SERVER_URL_WS || "localhost:5000";

// // const socket = new WebSocket(`ws://${URL}/ws`);
// // const socket = new WebSocket(`ws://${URL}/ws`);

// // export default socket;
